node -v
npm -v
npm init
npm init -y (npm 초기화)
npm install jquery (npm 설치)
npm install jquery-ui
npm install gulp
npm uninstall gulp
npm install gulp --global (전역 설치)
npm install vue --save-prod (배포용)
=npm i vue
npm install webpack --save-dev (개발용)
=npm i webpack -D

npm의 장점
1. 라이브러리의 목록과 버전을 확인할 수 있음.
2. 해당 cdn을 불러오지 않아도 install로 설치할 경우 로컬에 필요한 라이브버리 설치해서 사용 가능.